import express from "express"

const router = express.Router()

// Menu is static JSON on the frontend — no routes needed here yet
// Stripe / payment routes go in payments.js when ready

export default router